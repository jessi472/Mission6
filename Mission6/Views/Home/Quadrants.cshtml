@{
    ViewData["Title"] = "Quadrants";
}

@model List<Task>

<div class="container quadrants-grid">
    <div class="quadrant">
        <h3>Important / Urgent</h3>
        <ul>
            @foreach (var task in CoveyForm)
            {
                <li><input type="checkbox" asp-for="Completed" value="" /></li>
                <li><input type="text" asp-for="DueDate" value="" /></li>
                <li><input type="text" asp-for="Task" value="" /></li>
                <li><select type="text" asp-for="CategoryId" value="" />
                @foreach (var category in Category)
                {
                    <option>@category</option>
                }
            </li>
            }
        </ul>
    </div>
    <div class="quadrant">
        <h3>Important / Not Urgent</h3>
        <ul>
            @foreach (var task in CoveyForm)
            {

            }
        </ul>
    </div>
    <div class="quadrant">
        <h3>Not Important / Urgent</h3>
        <ul>
            @foreach (var task in CoveyForm)
            {

            }
        </ul>
        
    </div>
    <div class="quadrant">
        <h3>Not Important / Not Urgent</h3>
        <ul>
            @foreach (var task in CoveyForm)
            {

            }
        </ul>
    </div>
</div>